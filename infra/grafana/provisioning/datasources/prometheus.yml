# Datasource provisionado: o Grafana já sobe sabendo onde está o Prometheus,
# sem ninguém precisar clicar em nada.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
